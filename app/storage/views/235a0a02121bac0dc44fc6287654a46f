<?php $__env->startSection("content"); ?>
<div>
    <ol class="breadcrumb">
      <li><a href="<?php echo e(URL::route('user.home')); ?>"><?php echo trans('messages.home'); ?></a></li>
      <li class="active">Health Units</li>
    </ol>
</div>
<?php if(Session::has('message')): ?>
    <div class="alert alert-info"><?php echo Session::get('message'); ?></div>
<?php endif; ?>
<div class="panel panel-primary">
    <div class="panel-heading ">
        <span class="glyphicon glyphicon-adjust"></span>
        Health Units
        <div class="panel-btn">
            <a class="btn btn-sm btn-info" href="<?php echo URL::to("ward/create"); ?>" >
                <span class="glyphicon glyphicon-plus-sign"></span>
                Create Health Unit
            </a>
        </div>
    </div>
    <div class="panel-body">
        <table class="table table-striped table-hover table-condensed search-table">
            <thead>
                <tr>
                    <th><?php echo Lang::choice('messages.name',1); ?></th>
                    <th><?php echo trans('messages.description'); ?></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
            <?php foreach($ward as $key => $value): ?>
                <tr>
                    <td><?php echo $value->name; ?></td>
                    <td><?php echo $value->description; ?></td>
                    
                    <td>

                    <!-- show the ward (uses the show method found at GET /ward/{id} -->
                        <a class="btn btn-sm btn-success" href="<?php echo URL::to("ward/" . $value->id); ?>" >
                            <span class="glyphicon glyphicon-eye-open"></span>
                            <?php echo trans('messages.view'); ?>

                        </a>

                    <!-- edit this ward (uses edit method found at GET /ward/{id}/edit -->
                        <a class="btn btn-sm btn-info" href="<?php echo URL::to("ward/" . $value->id . "/edit"); ?>" >
                            <span class="glyphicon glyphicon-edit"></span>
                            <?php echo trans('messages.edit'); ?>

                        </a>
                        
                    <!-- delete this ward (uses delete method found at GET /ward/{id}/delete -->
                        <?php echo Form::open(['route' => ['ward.destroy', $value->id], 'method' => 'DELETE',
                            'style' => 'display: inline-block;']); ?>

                        <button class="btn btn-sm btn-danger">
                            <span class="glyphicon glyphicon-trash"></span>
                            <?php echo trans('messages.delete'); ?>

                        </button>
                        <?php echo Form::close(); ?>

                    </td>
                </tr>
            <?php endforeach; ?>
            </tbody>
        </table>
    </div>
</div>
<?php $__env->stopSection(); ?>

<?php echo $__env->make("layout", array_except(get_defined_vars(), array('__data', '__path')))->render(); ?>